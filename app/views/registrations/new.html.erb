<div class="min-h-screen flex flex-col">
  <div class="flex-1 flex items-center justify-center bg-surface py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8">
      <div class="text-center">
        <h1 class="text-3xl font-bold text-primary mb-2">加入人脉高手社区</h1>
        <p class="text-secondary">填写信息完成注册，审核通过后即可使用</p>
      </div>

      <div class="card">
        <div class="card-body">
          <%= render 'registrations/signup_form_with_oauth' %>
        </div>
      </div>

      <div class="text-center">
        <p class="text-sm text-secondary">
          已有账户？
          <%= link_to "立即登录", sign_in_path, class: "font-medium text-primary hover:text-primary-dark" %>
        </p>
      </div>
    </div>
  </div>

  <!-- Builder Attribution -->
  <%= render 'shared/footer_attribution' %>
</div>