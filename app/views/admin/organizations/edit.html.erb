<% content_for :title, "Edit Organization" %>

<div class="container-md">
  <!-- Page Title -->
  <div class="flex items-center space-x-4 mb-6">
    <%= link_to admin_organization_path(@organization.id), class: "btn-ghost text-sm py-1 px-3" do %>
      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back to Organization
    <% end %>
    <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50">Edit Organization</h1>
  </div>

  <!-- Form -->
  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <%= form_with model: [:admin, @organization], local: true, class: "space-y-6" do |form| %>
        <%= render 'shared/form_error_message', model: @organization %>

        <!-- Name -->
        <div class="mb-4">
          <%= form.label :name, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Name<% if field_required?(@organization, :name) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :name, {
            placeholder: "Enter name...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@organization, :name) ? { required: true } : {}) %>
          <% if @organization.errors[:name].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @organization.errors[:name].first %></p>
          <% end %>
        </div>

        <!-- Description -->
        <div class="mb-4">
          <%= form.label :description, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Description<% if field_required?(@organization, :description) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_area :description, {
            rows: 5,
            placeholder: "Enter description...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@organization, :description) ? { required: true } : {}) %>
          <% if @organization.errors[:description].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @organization.errors[:description].first %></p>
          <% end %>
        </div>

        <!-- Admin user -->
        <div class="mb-4">
          <%= form.label :admin_user_id, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Admin user<% if field_required?(@organization, :admin_user) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :admin_user_id, 
              options_for_select([['Select Admin user', '']] + User.all.map { |item| [item.name, item.id] }, @organization.admin_user_id),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @organization.errors[:admin_user_id].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @organization.errors[:admin_user_id].first %></p>
          <% end %>
        </div>

        <!-- Logo -->
        <div class="mb-4">
          <%= form.label :logo, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Logo
          <% end %>
          
          <!-- Current attachments -->
          <% if @organization.logo.attached? %>
            <div class="mb-3 p-3 border border-gray-200 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-800">
              <p class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Current file:</p>
              <div class="text-sm text-gray-600 dark:text-gray-400 flex items-center">
                <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4z" clip-rule="evenodd"/>
                </svg>
                <%= @organization.logo.filename %>
              </div>
            </div>
          <% end %>
          
          <%= form.file_field :logo, {
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100"
          } %>
          <p class="text-sm text-gray-500 mt-1">Select a new file to replace the current one, or leave empty to keep current file</p>
          <% if @organization.errors[:logo].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @organization.errors[:logo].first %></p>
          <% end %>
        </div>

        <!-- Background image -->
        <div class="mb-4">
          <%= form.label :background_image, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Background image
          <% end %>
          
          <!-- Current attachments -->
          <% if @organization.background_image.attached? %>
            <div class="mb-3 p-3 border border-gray-200 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-800">
              <p class="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Current file:</p>
              <div class="text-sm text-gray-600 dark:text-gray-400 flex items-center">
                <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h4.586A2 2 0 0112 2.586L15.414 6A2 2 0 0116 7.414V16a2 2 0 01-2 2H6a2 2 0 01-2-2V4z" clip-rule="evenodd"/>
                </svg>
                <%= @organization.background_image.filename %>
              </div>
            </div>
          <% end %>
          
          <%= form.file_field :background_image, {
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100"
          } %>
          <p class="text-sm text-gray-500 mt-1">Select a new file to replace the current one, or leave empty to keep current file</p>
          <% if @organization.errors[:background_image].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @organization.errors[:background_image].first %></p>
          <% end %>
        </div>

        <!-- Form Actions -->
        <div class="flex justify-end space-x-3">
          <%= link_to admin_organization_path(@organization.id), class: "btn-ghost" do %>
            Cancel
          <% end %>
          <%= form.submit "Update Organization", class: "btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>