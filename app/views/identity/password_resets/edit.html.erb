<div class="container-sm flex items-center justify-center flex-1 py-12">
  <div class="card w-full">
    <div class="card-body">
      <h1 class="text-3xl font-bold text-center text-primary mb-8">重置密码</h1>

      <%= form_with model: @user, url: identity_password_reset_path(token: params[:token]), method: :patch, local: true, class: "space-y-6" do |form| %>
        <%= render 'shared/form_error_message', model: @user %>

        <div>
          <%= form.label :password, class: "form-label" do %>
            新密码
          <% end %>
          <%= form.password_field :password, required: true, autofocus: true, autocomplete: "new-password", class: "form-input" %>
        </div>

        <div>
          <%= form.label :password_confirmation, class: "form-label" do %>
            确认密码
          <% end %>
          <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", class: "form-input" %>
        </div>

        <div class="pt-2">
          <%= form.submit "保存修改", class: "btn-primary w-full btn-lg" %>
        </div>
      <% end %>

      <div class="text-center mt-8">
        <%= link_to "返回登录", sign_in_path, class: "text-primary hover:text-primary-dark underline font-medium transition-colors" %>
      </div>
    </div>
  </div>
</div>